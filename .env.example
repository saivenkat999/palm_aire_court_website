# Environment Variables Template
# Copy this file to .env and fill in your production or staging values

### Core runtime ------------------------------------------------------------
# Railway automatically injects PORT; keep for local/dev parity
PORT=5000
NODE_ENV=production

### Supabase (Database & Backend) -------------------------------------------
# Get these from your Supabase project settings: https://app.supabase.com
# Project Settings -> API -> Project URL
SUPABASE_URL="https://your-project.supabase.co"

# Project Settings -> API -> Project API keys -> anon/public key
SUPABASE_ANON_KEY="your-anon-key-here"

# Optional: Service role key for admin operations (keep secret!)
# SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-here"

### Stripe (payments) --------------------------------------------------------
# Replace with LIVE keys before going live
STRIPE_SECRET_KEY="sk_live_XXXXXXXXXXXXXXXXXXXXXXXX"
STRIPE_PUBLISHABLE_KEY="pk_live_XXXXXXXXXXXXXXXXXXXXXXXX"

### GoHighLevel (CRM) --------------------------------------------------------
# Optional – only required if CRM sync is enabled
GHL_API_KEY="your_gohighlevel_api_key"

### Application --------------------------------------------------------------
# Long random string for cookie/session security
SESSION_SECRET="replace-with-64-char-random-string"
